{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Tetris (LLDB)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/tetrismain",
            "args": [],
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "lldb",
            "preLaunchTask": "Build Tetris"
        }
    ]
}
